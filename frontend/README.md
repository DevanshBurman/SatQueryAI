# Frontend

`src/` is the React application; `public/` contains static assets and standalone motion pages. `index.html` is the Vite entry point. Run `npm run dev` or `npm run build` from the repository root, where the shared dependencies and Vercel configuration live.
